[project]
name = "band-networks"
version = "0.1.0"
description = "Bipartite graph of Seattle grunge scene bands and musicians"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "musicbrainzngs>=0.7.1",
    "fastapi>=0.115.0",
    "uvicorn>=0.32.0",
]
